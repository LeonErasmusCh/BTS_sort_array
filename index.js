const test = require('./bts');

let arr = [1, -1, 0, 2, -2];
//console.log(test.order(arr));

test.order(arr);

/*  bts.js will order the numbers in arr=[]

1. charge values of arr = [];
2. Run: node index.js 

*/
